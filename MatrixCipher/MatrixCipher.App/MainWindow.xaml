<Window x:Class="MatrixCipher.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Matrix Cipher App" Height="350" Width="500"
        ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Поле ввода текста -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Исходный текст:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtInput" Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" ToolTip="Введите текст для шифрования"/>
        </StackPanel>

        <!-- Настройки матрицы -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <StackPanel Margin="0,0,10,0" Width="100">
                <TextBlock Text="Строки (rows):" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox x:Name="TxtRows" Text="3" ToolTip="Целое число > 0"/>
            </StackPanel>
            <StackPanel Width="100">
                <TextBlock Text="Столбцы (cols):" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox x:Name="TxtCols" Text="4" ToolTip="Целое число > 0"/>
            </StackPanel>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button x:Name="BtnEncrypt" Content="Зашифровать" Width="100" Margin="0,0,5,0" Click="BtnEncrypt_Click"/>
            <Button x:Name="BtnDecrypt" Content="Расшифровать" Width="100" Margin="0,0,5,0" Click="BtnDecrypt_Click"/>
            <Button x:Name="BtnClear" Content="Очистить" Width="80" Click="BtnClear_Click"/>
        </StackPanel>

        <!-- Результат -->
        <StackPanel Grid.Row="3">
            <TextBlock Text="Результат:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtOutput" Height="80" TextWrapping="Wrap" IsReadOnly="True" Background="#F0F0F0" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>
    </Grid>
</Window>